// This is a short code for the "clear highscores" button itself, to remove any errors from the console for the sake of other html pages involved

// Call the function to clear scores from the highscores list
    clearBtn.addEventListener("click", function() {
        clearScores();
    })
